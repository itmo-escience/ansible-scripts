PORT=5050
IP={{ansible_eth1.ipv4.address}}
CLUSTER={{mesos_cluster_name}}
LOG_DIR=/var/log/mesos
WORK_DIR=/var/run/mesos
MESOS_HOSTNAME={{ hostvars[groups['mesos_masters'][0]]['ansible_hostname'] }}
{% if mesos_cluster_mode == "zookeeper" %}
ZK=zk://{{ groups.zookeeper | join(':' + '2181' + ',') }}:2181/mesos
{% endif %}